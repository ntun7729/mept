{
  "name": "mept-english-trainer",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "MEPT English trainer web app with OpenAI-compatible question generation, audio listening practice, and answer checking.",
  "scripts": {
    "dev": "node server.js",
    "dev:debug": "LOG_LEVEL=debug node server.js",
    "start": "node server.js",
    "check": "node --check server.js && node --check src/ai-client.js && node --check src/question-service.js && node --check src/check-service.js && node --check src/mept-format.js && node --check src/logger.js && node --check src/runtime-env.js && node --check \"functions/api/[[path]].js\"",
    "pages:dev": "wrangler pages dev public",
    "deploy": "wrangler pages deploy public --project-name=mept"
  },
  "dependencies": {
    "dotenv": "latest",
    "express": "latest"
  },
  "devDependencies": {
    "wrangler": "latest"
  },
  "engines": {
    "node": ">=18.17"
  }
}
